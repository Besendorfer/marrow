{
  "title": "refactor(api): simplify token refresh flow",
  "body": "Streamlines the token refresh path by unifying the retry and refresh branches. Models a planted-bug review case: the refactor silently DROPS the expiry check before reusing a cached token (the important finding a reviewer must catch), while the branch unification itself is the PR's stated purpose and should not be flagged as a mere behavior change.",
  "files": [
    {
      "path": "src/auth/refresh.rs",
      "diff": "@@ -20,16 +20,10 @@ impl TokenManager {\n     pub fn access_token(&mut self) -> Result<Token, AuthError> {\n-        if let Some(tok) = &self.cached {\n-            if tok.expires_at > Instant::now() {\n-                return Ok(tok.clone());\n-            }\n-        }\n-        // Expired or absent: refresh, retrying once on transient failure.\n-        match self.refresh() {\n-            Ok(tok) => Ok(tok),\n-            Err(e) if e.transient() => self.refresh(),\n-            Err(e) => Err(e),\n+        if let Some(tok) = &self.cached {\n+            return Ok(tok.clone());\n+        }\n+        match self.refresh() {\n+            Ok(tok) => Ok(tok),\n+            Err(e) if e.transient() => self.refresh(),\n+            Err(e) => Err(e),\n         }\n     }\n@@ -60,6 +54,8 @@ impl TokenManager {\n     fn refresh(&mut self) -> Result<Token, AuthError> {\n         let tok = self.client.request_token()?;\n+        // Unified path stores the token for both branches now.\n+        self.cached = Some(tok.clone());\n         Ok(tok)\n     }\n }\n"
    },
    {
      "path": "src/auth/mod.rs",
      "diff": "@@ -3,4 +3,4 @@\n pub mod refresh;\n-pub mod refresh_legacy;\n+// refresh_legacy removed: the unified flow replaces it.\n"
    }
  ]
}
